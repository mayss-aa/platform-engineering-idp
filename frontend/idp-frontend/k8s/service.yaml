apiVersion: v1
kind: Service
metadata:
  name: idp-frontend
  namespace: idp-platform
  labels:
    app: idp-frontend
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: idp-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
